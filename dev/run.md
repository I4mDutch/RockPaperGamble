# In one terminal (Server)
cd packages/server && npx partykit dev

# In another terminal (Frontend)
cd packages/frontend && npm run dev


# When The Server (Cloudflare Pages) Version Is Not Working
- When the custom money/ready up/event feed/re-order arrows dont work

cd packages/server && npx partykit deploy
